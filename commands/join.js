const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

    return message.channel.send("Join zelf corona verkrachte pedofiel !");

}

module.exports.help = {
    name: "join"
}