const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

    return message.channel.send("Bot is gender neutraal😂 a verkrachte dildo dat je bent !");

}

module.exports.help = {
    name: "Kankerhoer"
}