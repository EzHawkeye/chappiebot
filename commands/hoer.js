const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

    return message.channel.send("Omdat jou zebi moeder een kanker slet is !");

}

module.exports.help = {
    name: "hoer"
}