const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

    return message.channel.send("Hawk is een pro, een legend, een toffe gast & natuurlijk vind ik het leuk om je helemaal de KANKER IN TE SCHELDEN !");

}

module.exports.help = {
    name: "hawk"
}