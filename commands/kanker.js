const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

    return message.channel.send("Je hele kanker familie heeft kanker !");

}

module.exports.help = {
    name: "kanker"
} 